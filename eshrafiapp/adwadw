<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <title>إشرافي</title>
  <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --main-bg: #e0f2f1;
      --card-bg: #ffffff;
      --text-color: #000000;
      --topbar-bg: #2e8b57;
      --button-bg: #2e8b57;
      --button-hover: #246b45;
    }

    body.dark-mode {
      --main-bg: #1e1e1e;
      --card-bg: #2c2c2c;
      --text-color: #f0f0f0;
      --topbar-bg: #444;
      --button-bg: #555;
      --button-hover: #666;
    }

    body {
      font-family: 'Tajawal', sans-serif;
      margin: 0;
      background-color: var(--main-bg);
      color: var(--text-color);
      height: 100vh;
      overflow: hidden;
      direction: rtl;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .top-bar {
      position: fixed;
      top: 0;
      right: 0;
      left: 0;
      height: 55px;
      background-color: var(--topbar-bg);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      font-weight: bold;
      z-index: 100;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .mode-toggle {
      position: absolute;
      left: 20px;
      background: none;
      border: none;
      color: white;
      font-size: 18px;
      cursor: pointer;
      padding: 6px 10px;
      border-radius: 6px;
      transition: background 0.3s ease;
    }

    .mode-toggle:hover {
      background-color: rgba(255,255,255,0.1);
    }

    .splash {
      position: absolute;
      top: 55px;
      left: 0;
      width: 100%;
      height: calc(100% - 55px);
      background-color: var(--card-bg);
      display: flex;
      justify-content: center;
      align-items: center;
      animation: fadeOut 1s ease-in-out forwards;
      animation-delay: 2.5s;
      z-index: 10;
    }

    .splash .logo-row {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 40px;
    }

    .splash img {
      width: 120px;
      opacity: 0;
      animation: fadeIn 1s ease forwards;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    .splash img:nth-child(1) { animation-delay: 0.3s; }
    .splash img:nth-child(2) { animation-delay: 0.6s; }
    .splash img:nth-child(3) { animation-delay: 0.9s; }

    @keyframes fadeIn {
      to { opacity: 1; transform: scale(1.05); }
    }

    @keyframes fadeOut {
      to { opacity: 0; visibility: hidden; }
    }

    .login-box {
      display: none;
      position: relative;
      z-index: 1;
      background-color: var(--card-bg);
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0,0,0,0.08);
      width: 100%;
      max-width: 400px;
      margin: auto;
      top: 50%;
      transform: translateY(-50%);
      text-align: center;
      transition: background-color 0.3s ease;
    }

    .login-box img.logo {
      width: 80px;
      margin-bottom: 20px;
    }

    .login-box h2 {
      margin-bottom: 30px;
      color: var(--topbar-bg);
      font-size: 22px;
    }

    input {
      width: 100%;
      padding: 12px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 16px;
      box-sizing: border-box;
      background-color: #fff;
      color: #000;
    }

    body.dark-mode input {
      background-color: #444;
      color: #fff;
      border: 1px solid #666;
    }

    button {
      width: 100%;
      padding: 12px;
      background-color: var(--button-bg);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: var(--button-hover);
    }

    .error {
      color: red;
      font-size: 14px;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <!-- ✅ الشريط العلوي الأخضر -->
  <div class="navbar">
    <button onclick="toggleDarkMode()">🌙</button>
    <div class="center"><strong>إشرافي</strong></div>
    <button onclick="logout()">🔓</button>
  </div>

  <!-- ✅ شاشة الترحيب -->
  <div class="splash" id="splashScreen">
    <div class="logo-row">
      <img src="ministry-logo.png" alt="شعار الوزارة">
      <img src="logo.png" alt="شعار إشرافي">
      <img src="school-logo.png" alt="شعار المدرسة">
    </div>
  </div>

  <!-- ✅ نموذج تسجيل الدخول -->
  <div class="login-box" id="loginBox">
    <img src="logo.png" alt="شعار إشرافي" class="logo">
    <h2>تسجيل الدخول إلى إشرافي</h2>

    <input type="text" id="username" placeholder="اسم المستخدم">
    <input type="password" id="password" placeholder="كلمة المرور">
    <button onclick="login()">دخول</button>

    <div class="error" id="errorMsg"></div>
  </div>

  <script>
    setTimeout(() => {
      document.getElementById("loginBox").style.display = "block";
    }, 2500);

    function toggleMode() {
      document.body.classList.toggle("dark-mode");
      localStorage.setItem("theme", document.body.classList.contains("dark-mode") ? "dark" : "light");
    }

    window.onload = () => {
      if (localStorage.getItem("theme") === "dark") {
        document.body.classList.add("dark-mode");
      }
    };

    function login() {
      const username = document.getElementById("username").value.trim();
      const password = document.getElementById("password").value.trim();
      const errorMsg = document.getElementById("errorMsg");

      if (!username || !password) {
        errorMsg.textContent = "يرجى تعبئة اسم المستخدم وكلمة المرور";
        return;
      }

      const users = {
        "teacher1": { password: "1234", role: "teacher", name: "أستاذ" },
        "agent1":   { password: "1234", role: "agent", name: "وكيل" },
        "manager1": { password: "1234", role: "manager", name: "مدير" }
      };

      const user = users[username];

      if (user && user.password === password) {
        localStorage.setItem("teacherName", user.name);

        if (user.role === "teacher") {
          window.location.href = "ta.html";
        } else if (user.role === "agent") {
          window.location.href = "agent.html";
        } else if (user.role === "manager") {
          window.location.href = "manager.html";
        }
      } else {
        errorMsg.textContent = "بيانات الدخول غير صحيحة";
      }
    }

        function toggleDarkMode() {
      document.body.classList.toggle("dark-mode");
      localStorage.setItem("theme", document.body.classList.contains("dark-mode") ? "dark" : "light");
    }

    window.onload = () => {
      if (localStorage.getItem("theme") === "dark") {
        document.body.classList.add("dark-mode");
      }
    };

    function logout() {
      localStorage.clear();
      location.replace("index.html");
    }
  </script>
</body>
</html>